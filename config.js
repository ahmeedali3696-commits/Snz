/* ---------------------------------------------------   SNZ PLATFORM - SECURED CONFIGURATION V2--------------------------------------------------- */// 1. إعدادات الاتصال بقاعدة بيانات Supabaseconst supabaseUrl = 'https://yebcshcdjbwzlpujpkjx.supabase.co';const supabaseKey = 'sb_publishable_LOJTDkfGK0Xe5_lNlWNduQ_X16ToqeJ';// إنشاء الاتصال بقاعدة البياناتconst _supabase = window.supabase.createClient(supabaseUrl, supabaseKey);console.log("...SYSTEM: DATABASE NODE CONNECTED SUCCESSFULLY");// 2. نظام فحص الصيانة الذكي وحماية التوجيهasync function checkGlobalMaintenance() {    if (typeof _supabase === 'undefined') {        console.error("خطأ: لم يتم تحميل مكتبة Supabase بنجاح.");        return;    }        try {        // جلب حالة الصيانة الحالية من قاعدة البيانات        const { data, error } = await _supabase.from('settings').select('is_maintenance').single();        if (error) throw error;         // قراءة مسار الصفحة الحالية وتحويله لحروف صغيرة (يحل مشاكل الروابط في Vercel)        const path = window.location.pathname.toLowerCase();                // 🛡️ حماية لوحة التحكم: إذا كان الرابط يحتوي على كلمة admin، اسمح بالدخول فوراً وتجاهل الصيانة        if (path.includes('admin')) {            console.log("Admin Bypass: تم تخطي فحص الصيانة للوحة الإدارة.");            return;         }                // 🔄 توجيه الزوار العاديين        if (data && data.is_maintenance === true) {            // إذا الموقع في وضع الصيانة، والزائر ليس في صفحة الصيانة أصلاً            if (!path.includes('maintenance')) {                window.location.replace('/maintenance.html');            }        }         else if (data && data.is_maintenance === false) {            // إذا الموقع يعمل بشكل طبيعي، والزائر عالق في صفحة الصيانة            if (path.includes('maintenance')) {                window.location.replace('/index.html');            }        }    } catch(e) {        console.error("System Check Failed:", e.message);    }}// تشغيل الفحص فور تحميل الملفcheckGlobalMaintenance();